# 文件位置：Quartz项目/.pre-commit-config.yaml

repos:
-   repo: local
    hooks:
    -   id: update-submodules
        name: Update content submodule
        entry: git submodule update --remote --merge
        language: system
        stages: [pre-commit]
        always_run: true
        pass_filenames: false